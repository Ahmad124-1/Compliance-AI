'use client';

import { Card } from '@/components/ui/card';

export default function ProgramNewPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Create new program form.</p>
    </Card>
  );
}