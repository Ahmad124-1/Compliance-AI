'use client';

import { Card } from '@/components/ui/card';

export default function ProgramDetailPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Program details page.</p>
    </Card>
  );
}