'use client';

import { Card } from '@/components/ui/card';

export default function ReportNewPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Generate new report form.</p>
    </Card>
  );
}