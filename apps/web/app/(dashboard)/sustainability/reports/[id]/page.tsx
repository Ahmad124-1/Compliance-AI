'use client';

import { Card } from '@/components/ui/card';

export default function ReportDetailPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Report details page.</p>
    </Card>
  );
}