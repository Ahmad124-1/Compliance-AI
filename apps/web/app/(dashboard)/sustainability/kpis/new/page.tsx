'use client';

import { Card } from '@/components/ui/card';

export default function KpiNewPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Create new KPI form.</p>
    </Card>
  );
}