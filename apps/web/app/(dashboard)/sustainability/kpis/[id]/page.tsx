'use client';

import { Card } from '@/components/ui/card';

export default function KpiDetailPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">KPI details page.</p>
    </Card>
  );
}