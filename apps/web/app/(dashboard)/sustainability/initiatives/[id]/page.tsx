'use client';

import { Card } from '@/components/ui/card';

export default function InitiativeDetailPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Initiative details page.</p>
    </Card>
  );
}