'use client';

import { Card } from '@/components/ui/card';

export default function GoalNewPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Create new goal form.</p>
    </Card>
  );
}