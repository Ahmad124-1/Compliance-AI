'use client';

import { Card } from '@/components/ui/card';

export default function GoalDetailPage() {
  return (
    <Card className="p-6">
      <p className="text-sm text-[rgb(var(--muted))]">Goal details page.</p>
    </Card>
  );
}